# Optional one-time administrator bootstrap. Replace before first startup.
WORKHUB_BOOTSTRAP_ADMIN_USERNAME=admin
WORKHUB_BOOTSTRAP_ADMIN_PASSWORD=change-me-admin-password
WORKHUB_SESSION_SECRET=change-me-random-session-secret

# Compose supplies fixed paths, ports and Mock OA demo values.
# Configure Chat, Embedding, Feishu and runtime intervals in the Web settings page.
